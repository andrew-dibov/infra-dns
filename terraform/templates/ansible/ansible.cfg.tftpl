[defaults]
inventory = ./inventory/hosts.yaml
ssh_args = -F ${auth_dir}/ssh_config -o ControlMaster=auto -o ControlPersist=30m
host_key_checking = False

[ssh_connection]
ssh_args = -F ${auth_dir}/ssh_config -o ControlMaster=auto -o ControlPersist=30m -o ServerAliveInterval=60